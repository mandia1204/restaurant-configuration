version: "2"
services:
 dashboard:
  build:
   context: "${DASHBOARD_BUILD_DOCKERFILE}"
  container_name: "${DASHBOARD_CONTAINER_NAME}"
  ports:
  - "${DASHBOARD_PORT}"
  volumes:
  - "${DASHBOARD_VOLUM_SRC}:/var/www/aspnetcoreapp"
