version: "2"
services:
 dashboard_common:
  build:
   context: "${DASHBOARD_BUILD_DOCKERFILE_CTX}"
   dockerfile: "${DASHBOARD_BUILD_DOCKERFILE_NAME}"
  container_name: "${DASHBOARD_CONTAINER_NAME}"
  ports:
  - "${DASHBOARD_PORT}"
