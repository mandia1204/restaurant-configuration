- name: create service
  k8s:
    state: present
    definition: "{{ lookup('file', '~/k8s/dashboard-be/service.yml') | from_yaml }}"

- name: create deployment
  k8s:
    state: present
    definition: "{{ lookup('template', '~/k8s/dashboard-be/deployment.yml') | from_yaml }}"
